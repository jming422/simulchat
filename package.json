{
  "name": "simulchat",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "14.x"
  },
  "cacheDirectories": [
    "node_modules"
  ],
  "dependencies": {
    "@emotion/core": "^10.0.28",
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "axios": "^0.19.2",
    "db-migrate": "^0.11.11",
    "db-migrate-pg": "^1.2.2",
    "http-proxy-middleware": "^1.0.5",
    "lodash": "^4.17.19",
    "pg": "^8.3.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1",
    "react-use-websocket": "^2.1.1",
    "uuid": "^8.3.0"
  },
  "devDependencies": {
    "dotenv": "^8.2.0"
  },
  "scripts": {
    "start": "PORT=3001 react-scripts start",
    "build": "rm -rf resources/build && react-scripts build && mkdir -p resources && mv build resources/build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "migrate": "db-migrate"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
